import { describe, expect, it } from "vitest";
import { useEditBelongings } from "@/hooks/useEditBelongings";

describe("useEditBelongings", () => {
  it("should be defined", () => {
    expect(useEditBelongings).toBeDefined();
  });
});
